# 理伴 Android 现有项目文档

## 1. 项目概况

理伴是一款运行在 Android 15/16 设备上的消费决策助手。用户开启识屏模式后，可以在购物应用、网页或外卖页面点击悬浮球。应用读取当前屏幕，在设备内识别商品名称、型号和价格，并结合预算、储蓄目标、历史消费与促销信号给出消费建议。

当前工程已经从固定 Demo 套壳改造成真实功能链路，不再包含 Demo A/B/C、Fixture 数据或静默演示回退。

基本信息：

- 应用名称：理伴
- Application ID：`com.liban.android`
- 开发语言：Kotlin
- UI：Jetpack Compose
- 悬浮窗：Android 原生 View
- 最低系统：Android 15，`minSdk 35`
- 编译目标：Android API 36.1
- `targetSdk 36`
- 构建系统：AGP 9.4.0、Gradle Wrapper 9.6.0

## 2. 当前功能

### 2.1 屏幕捕获

`FloatingCaptureService` 负责：

- 启动 MediaProjection 前台服务。
- 维持系统常驻通知和可拖动悬浮球。
- 用户点击悬浮球后隐藏全部 Overlay，等待两个显示帧，再读取最新画面。
- 检测空帧和近全黑帧。
- 处理屏幕尺寸变化、投屏停止、锁屏回收和 Service 销毁。
- 禁止同一时刻发起多个分析。

截图只作为内存中的 `Bitmap` 使用，不压缩成网络图片，不写入相册、文件、Room 或日志。

### 2.2 端侧 OCR

工程使用随 APK 内置的：

`com.google.mlkit:text-recognition-chinese:16.0.1`

OCR 输出：

- 文字内容
- 文字框坐标
- 文字框尺寸
- 元素平均置信度

OCR 完成后立即回收原始 Bitmap。OCR 全文仅作为当前函数内的临时对象，不持久化。

### 2.3 商品页面解析

`SceneParser` 根据 OCR 文字和布局提取：

- 商品名称
- 完整型号
- 当前价格
- 原价
- 商品分类
- 限时、稀缺、折扣信号
- 商品名置信度
- 价格置信度

当前规则包括：

- 价格必须明确包含 `¥`、`￥` 或“元”。
- 优先选择“现价、到手价、售价、活动价、券后”等上下文中的金额。
- 降低“原价、划线价、专柜价”金额的当前价格评分。
- 商品标题优先选择当前价格上方、长度合理、包含型号且字号较大的文字。
- 商品名置信度低于 0.65，或价格置信度低于 0.80 时进入手动修正。
- OCR 失败不会上传图片，也不会调用视觉模型。

### 2.4 本地决策

以下决策能力全部在设备本地执行：

- Budget：计算本次金额占剩余预算比例。
- Goal：估算消费对储蓄目标造成的延期天数。
- History：统计同分类近 30 天消费次数。
- Impulse：根据限时、稀缺和折扣信号计算冲动指数。
- DecisionEngine：合成本地风险等级、建议和说明。

风险与建议不依赖 LLM。模型估价不能降低本地风险等级，也不能单独生成“建议购买”。

### 2.5 渐进式结果

当前分析状态为：

`Idle → Capturing → Recognizing → PreliminaryResult → EnrichingPrice → Result`

异常时可进入：

- `NeedsCorrection`
- `Error`

本地 Skill 完成后立即创建并保存初步决策，界面显示预算、目标、历史和冲动因素；价格区域显示“正在查询”。价格结果返回后使用同一个决策 ID 原位更新。

用户可以在价格查询期间点击“还是买、等等、放弃”。数据库更新不会覆盖已经写入的反馈。

### 2.6 价格搜索

当前只内置智谱协议适配器：

- 搜索：`POST /paas/v4/web_search`
- 网页阅读：`POST /paas/v4/reader`
- 默认引擎：`search_pro`
- 默认结果数：10
- `content_size=high`

搜索词由商品名称、品牌、完整型号和“售价 价格 购买”组成，最长 70 个字符。

价格证据规则：

- 只接受带人民币符号或“元”的价格。
- 搜索文字必须包含完整型号。
- 过滤二手、回收、配件、保护壳、维修、定金、月供、优惠差额等结果。
- 按域名和价格去重。
- 搜索摘要不足时，最多并行读取两个页面。
- 网页阅读总时间预算为 1.2 秒。
- 至少三个独立域名才生成搜索验证区间。
- 通过中位数过滤小于 0.6 倍或大于 1.6 倍的明显离群值。
- 参考下限和上限使用过滤后价格的 25% 与 75% 分位数。

### 2.7 文本模型估价

LLM 仅支持 OpenAI-compatible Chat Completions 文本接口。

调用条件：

- 搜索未配置；或
- 搜索证据不足三个独立来源。

发送内容仅包括：

- 商品结构
- 页面价格
- 最多三条必要的搜索标题和摘要

请求中不包含截图、图片 URL、Base64 或 OCR 全文。代码会主动拒绝出现 `image_url` 或 `base64` 的 LLM 请求体。

模型输出固定为：

- `KNOWN` 或 `UNKNOWN`
- 人民币分区间
- 0 到 1 的置信度
- 不超过 60 字的依据

模型不得输出商家、链接或“当前最低价”。格式错误会重试一次。

### 2.8 价格来源

结果使用以下来源类型：

- `SEARCH_VERIFIED`：至少三个独立实时来源。
- `SEARCH_ASSISTED_ESTIMATE`：有少量搜索证据，结合文本模型估价。
- `MODEL_ESTIMATE`：未获得搜索证据，使用模型已有知识估价。
- `UNAVAILABLE`：搜索与模型均无法可靠估价。

结果卡展示：

- 来源标签
- 参考价格区间
- 查询时间
- 有效样本数
- 是否来自缓存
- 最多三个来源链接
- 模型依据或失败原因

### 2.9 API 设置

“设置”页面支持：

- LLM Endpoint
- LLM API Key
- LLM 模型名
- 智谱 Base URL
- 智谱 API Key
- 搜索引擎
- 网页阅读开关
- 密钥显示和隐藏
- 单独测试 LLM
- 单独测试搜索
- 清除 LLM 配置
- 清除搜索密钥

普通配置保存在 DataStore。API Key 使用 Android Keystore 生成的 AES-GCM 密钥加密后保存。保存后配置通过 StateFlow 立即生效，不需要重启或重新编译。

`local.properties` 只保存 Android SDK 路径，服务配置不再进入 BuildConfig。

### 2.10 数据库

Room 当前版本为 v2，保存：

- `UserProfileEntity`
- `SavingGoalEntity`
- `TransactionEntity`
- `DecisionHistoryEntity`
- `ApiDiagnosticEntity`
- `PriceCacheEntity`

v1 到 v2 使用显式迁移：

- 为决策历史增加价格来源。
- 增加价格缓存表。
- 保留旧预算、目标、交易和决策历史。
- 旧决策在界面显示为“旧版记录”。

“还是买”通过单个 Room 事务完成反馈、交易创建和预算扣减，并通过决策 ID 保证幂等。

### 2.11 缓存和超时

- 搜索验证结果缓存 2 小时。
- 搜索辅助估价和模型估价缓存 24 小时。
- 智谱搜索硬超时 3 秒。
- 网页阅读总预算 1.2 秒。
- LLM 单次估价硬超时 5 秒。
- 整个价格增强阶段外层限制为 5 秒。
- 外部服务失败不会阻塞本地反馈。

### 2.12 诊断

设置页下方显示接口诊断：

- 是否已配置
- 最近一次成功时间
- 最近耗时
- HTTP 状态
- 最近错误

诊断信息不显示密钥、截图、OCR 全文或用户完整消费内容。

## 3. 工程结构

主要代码：

- `app/src/main/java/com/liban/android/capture/FloatingCaptureService.kt`：悬浮窗和持续捕获。
- `app/src/main/java/com/liban/android/ocr/OcrProvider.kt`：ML Kit OCR。
- `app/src/main/java/com/liban/android/ocr/SceneParser.kt`：商品和价格解析。
- `app/src/main/java/com/liban/android/agent/AgentOrchestrator.kt`：完整分析编排。
- `app/src/main/java/com/liban/android/agent/Providers.kt`：智谱和 OpenAI-compatible 适配器。
- `app/src/main/java/com/liban/android/agent/PriceEvidenceAnalyzer.kt`：价格证据过滤与区间计算。
- `app/src/main/java/com/liban/android/agent/DecisionEngine.kt`：确定性本地决策。
- `app/src/main/java/com/liban/android/config/ConfigRepository.kt`：DataStore 与 Keystore 配置。
- `app/src/main/java/com/liban/android/data/`：Room 数据库、DAO 和 Repository。
- `app/src/main/java/com/liban/android/ui/`：Compose 页面和 ViewModel。

接口依据：

- [智谱联网搜索.md](智谱联网搜索.md)
- [智谱网页阅读.md](智谱网页阅读.md)

## 4. 构建和测试

本机需要：

- Android SDK 36.1
- JDK 17 或 Android Studio Embedded JDK
- Gradle Wrapper 9.6.0

常用命令：

```powershell
.\gradlew.bat testDebugUnitTest
.\gradlew.bat lintDebug
.\gradlew.bat assembleDebug
.\gradlew.bat assembleDebugAndroidTest
```

中文路径遇到 JVM 编码问题时可运行：

```powershell
.\build-competition.ps1
```

当前验证结果：

- JVM 单元测试：30 个通过，0 失败。
- Android 主程序：构建通过。
- androidTest APK：构建通过。
- Android Lint：0 问题。
- Debug APK：已生成。
- 真机仪器测试：当前没有连接设备，尚未执行。

APK 路径：

`app/build/outputs/apk/debug/app-debug.apk`

## 5. 当前限制

- 尚未在 Android 15/16 真机完成端到端回归。
- Keystore、Room 迁移和 ML Kit 首次识别测试已经编写并编译，但尚未在设备上运行。
- 价格解析是确定性规则，部分搜索页面可能只有促销文案而没有可用价格。
- 没有完整型号的商品不会生成 `SEARCH_VERIFIED`，通常进入模型估价或不可用。
- 第一版只实现智谱搜索协议，不支持用户自定义任意搜索 JSON 映射。
- 第一版只实现 OpenAI-compatible Chat Completions 文本模型。
- Debug APK 约 82 MB，主要来自内置中文 OCR 模型和多 ABI 原生库；尚未配置 release ABI 拆分。
- 当前没有生产后端、账户体系、云同步、银行账单同步或第三方应用自动点击。

## 6. 安全注意事项

- 不要把真实 API Key 写入源码、测试、`local.properties` 或提交记录。
- 客户端直连只适合受控设备或原型阶段，应使用短期、限额、可轮换密钥。
- 已用于联调的测试密钥应轮换。
- 应用不会绕过 MediaProjection 授权，也不会自动操作第三方应用。
