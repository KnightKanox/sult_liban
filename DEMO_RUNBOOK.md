# 理伴比赛演示 Runbook

## 演示前检查

1. 安装 APK，确认真机为 Android 15/16，网络稳定，系统未开启极端省电模式。
2. 打开“诊断”页：实时演示时 LLM 与物价服务均应显示“已配置”；Fixture 演示允许未配置。
3. 首次启动填写预算和目标，允许通知与悬浮窗权限，并接受系统屏幕共享提示。
4. 确认常驻通知和“理伴一下”悬浮球可见。
5. 切换 Demo 前在首页点选 A/B/C；这会加载对应的比赛预算状态。

## 三个固定场景

### Demo A：明显冲动消费

1. 首页选择 Demo A，打开包含“¥899、限时优惠、仅剩 2 件”的准备页面。
2. 点击悬浮球。
3. 预期：`HIGH`、建议等待、24 小时；理由包含预算或冲动促销信号。

### Demo B：价格判断

1. 首页选择 Demo B，打开 Sony WH-1000XM6、价格 ¥2,899 的准备页面。
2. 点击悬浮球。
3. 预期：指出当前价格高于 ¥2,499–2,699 参考区间，并建议等待。

### Demo C：长期目标影响

1. 首页选择 Demo C，打开价格 ¥7,999 的笔记本页面。
2. 点击悬浮球。
3. 预期：同时展示预算压力和储蓄目标延期影响，并建议等待。

每个场景分别测试“还是买 / 等等 / 放弃”。“还是买”应在首页增加已消费金额，并在历史页同时出现交易和决策反馈。

## 实时调用证明

- 演示前至少成功执行一次 LLM 和物价请求。
- “诊断”页应显示最近成功时间、耗时和 HTTP 200。
- 结果卡无来源标签表示全实时；“部分实时数据”表示某一步回退；“演示回退数据”表示固定 Fixture。

## 故障恢复

- 悬浮球消失：回到理伴，重新点击“开启识屏模式”。
- 系统提示投屏停止：屏幕解锁后回到理伴重新授权；旧授权令牌不可复用。
- 黑屏或受保护页面：点击悬浮提示，手动输入商品名称、价格和分类。
- 网络超时：固定 Demo 自动使用带标签的 Fixture；普通商品进入错误/手动输入状态。
- 结果卡无法显示：结果仍保留在理伴首页，点击常驻通知返回应用。
- 完全重置：停止识屏服务，在系统设置撤销并重新授予悬浮窗权限，然后重新打开应用。

## 封板验收

- Demo A/B/C 各连续执行 3 次，无崩溃、无重复记账。
- 拒绝权限、锁屏、旋转、断网、429、畸形 JSON 均有明确状态或降级。
- 正常网络下单次分析目标小于 20 秒。
- APK 使用比赛密钥签名；外部 API 密钥为短期限额密钥，赛后轮换。
