# 理伴 Android 项目目标

## 1. 产品目标

理伴的目标是在用户准备消费的关键时刻，快速提供一个基于真实页面信息和个人财务状态的理性提醒。

产品不代替用户做决定，不代替专业财务建议，也不自动操作购物应用。它的作用是把预算压力、储蓄目标、重复消费、促销诱导和价格证据放在同一张结果卡中，帮助用户在付款前多一次确认。

## 2. 核心体验目标

完整体验应当是：

1. 用户首次设置月预算和储蓄目标。
2. 用户主动授予通知、悬浮窗和屏幕捕获权限。
3. 用户在第三方商品页面点击“理伴一下”。
4. 应用在设备内识别商品、型号、价格和促销信息。
5. 默认在 OCR 后由 LLM 提取商品字段并校验原文证据，提取上限 5 秒；关闭此功能时本地初判目标约 1 秒。
6. 应用在后台查询价格证据，结果卡保持可操作。
7. 最迟 5 秒内原位更新价格来源与参考区间，或明确显示不可用。
8. 用户选择“还是买、等等、放弃”。
9. 反馈写入历史；“还是买”同步写入交易并扣减预算。

## 3. 功能目标

### 3.1 识别

- 截图必须只在端侧处理。
- 必须支持中文购物页面。
- 必须识别商品名称、完整型号、当前价格和原价。
- 全屏 OCR 按阅读顺序保留行号送入一次文本 LLM；商品采用固定核心字段与按需可选属性，名称可重组原文词组，不能只有品牌。
- 必须区分已选规格与仅出现的选项，保留拼单/券后等价格条件；未明确的属性不猜测。
- 没有唯一型号或规格无法核对时，搜索结果只能作同类/模型参考，不宣称已验证同款。
- 必须识别限时、稀缺和折扣信号。
- 必须结合文字框位置、字号和上下文选择价格与标题。
- 置信度仅作为结果元数据，不触发手动确认；LLM 提取失败时自动回退本地识别，仅缺少有效商品名或价格时要求补充信息。
- OCR 失败不得上传图片或自动改用视觉模型。
- 默认允许全屏 OCR 文字和文字位置发送给文本 LLM 提取商品信息，可在设置关闭；不得持久化 OCR 全文。

### 3.2 本地判断

- 预算、目标、历史和冲动 Skill 必须离线可用。
- 没有任何 API 配置时仍可完成 OCR 和本地决策。
- 本地风险判断必须是确定性规则并可单元测试。
- 网络结果只能补充价格信息，不能覆盖个人预算和目标风险。
- 模型估价不能降低风险，也不能单独生成“建议购买”。

### 3.3 搜索与估价

- 搜索可靠时不得调用价格估算 LLM；OCR 商品提取属于独立阶段。
- 至少三个独立实时来源才可标记为 `SEARCH_VERIFIED`。
- 不足三个来源时可以结合搜索证据调用 LLM。
- 未配置搜索时可以使用 LLM 模型知识估价。
- 搜索和模型均失败时必须显示 `UNAVAILABLE`。
- 每个来源类型必须对用户清晰可见。
- 模型不得伪造商家、链接、实时最低价或搜索事实。

### 3.4 用户配置

- 用户可以在客户端自行配置 LLM 和智谱搜索。
- 配置保存后必须立即生效。
- API Key 必须使用 Android Keystore 加密保存。
- 用户必须能够显示、隐藏、替换和清除密钥。
- LLM 与搜索必须有独立连接测试。
- 配置和诊断页不得泄露密钥或用户截图。

### 3.5 数据

- 金额统一使用人民币分和 Long。
- 原始截图与 OCR 全文不得持久化。
- 反馈必须通过决策 ID 幂等。
- PURCHASE 必须在单个事务中写入反馈、交易和预算变化。
- 数据库升级必须保留旧预算、目标、交易和决策。
- 缓存必须保留来源类型和生成时间。

## 4. 性能目标

- 纯本地模式初步结果 P50 小于 1 秒；LLM 商品提取阶段最多增加 5 秒。
- 搜索完整结果 P90 小于 3 秒。
- 网页阅读总时间不超过 1.2 秒。
- 任意价格增强链路最迟 5 秒结束。
- 连续点击不得产生并发分析或重复反馈。
- 旋转、网络切换、进程重建和 API 故障不得造成崩溃或永久卡死。

## 5. 隐私与安全目标

- 截图不得离开设备。
- Bitmap、OCR 全文和原始图片不得进入 Room、日志或缓存。
- OCR 提取 LLM 接收全屏 OCR 文字和位置；估价 LLM 只接收商品信息和少量搜索摘要，不接收页面价格。
- 所有远程 Endpoint 默认要求 HTTPS；仅允许 localhost 用于开发。
- 客户端密钥必须短期、限额、可轮换。
- 真实密钥仅在被 Git 忽略的 `api-presets.local.properties` 中配置；按联调需要注入 BuildConfig/APK。不得进入受版本控制的源码、测试或 Git，分发 APK 前应换用独立限额密钥。
- MediaProjection 必须由用户明确授权，并遵守 Android 单会话约束。

## 6. 验收矩阵

| 场景 | 预期结果 |
|---|---|
| 无任何 API | OCR、本地 Skill 和反馈可用；价格为 `UNAVAILABLE` |
| 仅配置 LLM | 价格标记为 `MODEL_ESTIMATE` 或明确 `UNKNOWN` |
| 仅配置智谱 | 三个独立来源时为 `SEARCH_VERIFIED`；证据不足时不可用 |
| 同时配置搜索和 LLM | 搜索优先；可靠搜索不调用 LLM；不足时为 `SEARCH_ASSISTED_ESTIMATE` |
| OCR 低置信度 | 信息完整时自动继续分析、不弹确认，确认没有图片网络请求 |
| 智谱返回 401/429 | 本地结论和反馈保持可用，诊断显示状态 |
| 搜索超时 | 最迟 5 秒结束，显示估价或不可用 |
| LLM 畸形 JSON | 重试一次，仍失败则显示不可用 |
| 用户重复点击反馈 | 只记录一次，不重复扣减预算 |
| 数据库从 v1 升级 | 旧预算、目标、交易和决策全部保留 |
| 锁屏或投屏停止 | 捕获资源释放，提示用户重新授权 |

## 7. 测试目标

### JVM 单元测试

- 人民币金额解析。
- OCR 文字框排序。
- 多价格选择。
- 标题和型号匹配。
- 促销信号。
- 二手、配件、优惠差额和错误型号过滤。
- 来源去重和离群值过滤。
- 分位价格区间。
- Skill 边界。
- 未知 Skill。
- 模型估价不影响本地风险。
- LLM 请求体不包含图片或 Base64。
- LLM `KNOWN/UNKNOWN` 和 JSON 重试。

### MockWebServer

- 智谱搜索成功。
- 智谱网页阅读成功。
- 401、429 和超时。
- 空结果、重复来源和无有效价格。
- LLM 畸形 JSON、越界金额、伪造链接和重试失败。

### Android 仪器测试

- Keystore 加密保存与清除。
- DataStore 进程重建。
- Room v1 到 v2 迁移。
- PURCHASE 原子更新和幂等。
- 价格缓存过期。
- ML Kit 中文模型首次识别。
- 权限拒绝、撤销和重新授权。

### 真机测试

- Android 15 和 Android 16。
- 主流购物、网页和外卖页面。
- 横竖屏旋转。
- 锁屏和投屏停止。
- 受保护页面黑屏。
- Wi-Fi 与移动网络切换。
- 连续分析。
- 真实智谱搜索和至少一个 OpenAI-compatible 模型。

## 8. 近期交付目标

### P0：完成真机闭环

- 在 Android 15/16 真机安装当前 APK。
- 运行全部 Android 仪器测试。
- 用至少十个真实商品页面校准 OCR 解析。
- 验证搜索成功时不调用 LLM。
- 验证无网络和 API 错误下反馈仍然可用。
- 记录 P50 OCR、本地初判和 P90 搜索耗时。

### P1：提高搜索精度

- 扩展价格上下文识别。
- 对电商平台、新闻、评测站和厂商官网做来源分类。
- 避免把建议零售价、首发价和过期促销当作当前成交价。
- 增加发布日期和内容时效性判断。
- 为没有标准型号的商品设计独立的搜索可信度规则。

### P1：降低安装体积

- 生成 release 构建。
- 启用 ABI Split 或 Android App Bundle。
- 检查 ML Kit OCR 原生库占用。
- 在不破坏离线 OCR 的前提下优化包体。

### P2：稳定性与可维护性

- 增加 Repository 和 Orchestrator 的集成测试。
- 增加 Compose UI 自动化测试。
- 增加结构化诊断导出，但继续排除密钥、截图和 OCR 全文。
- 为不同搜索供应商预留新适配器，而不把业务层绑定到具体厂商。

## 9. 不在当前目标内

- 应用商店正式发布。
- 生产级服务端。
- 多用户账户体系。
- 云端数据同步。
- 银行账单自动同步。
- 自动点击或控制第三方应用。
- 自动下单和支付。
- 完整商品价格历史平台。
- 对 Android 14 及以下系统提供兼容保证。

## 10. 完成定义

项目达到阶段性完成，需要同时满足：

- 真机端截图从未进入网络。
- 十个真实页面中商品名、型号和价格识别达到可接受水平。
- 纯本地初步结论稳定在约 1 秒内出现；启用 LLM 提取后单独统计其耗时，最多等待 5 秒后自动使用本地识别结果，仅缺少必要信息时要求补充。
- 搜索可靠时不调用估价 LLM。
- 所有价格结果都有明确来源。
- 外部服务最迟 5 秒结束。
- 三种反馈均正确落库且幂等。
- 数据库迁移不丢失旧数据。
- Lint、JVM 测试和 Android 仪器测试全部通过。
- 真实 API Key 不存在于受版本控制的文件和 Git 历史；联调 APK 的预置密钥只来自被忽略的本机配置。
